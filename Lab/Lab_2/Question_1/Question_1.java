package CS203Course.Lab.Lab_2.Question_1;

public class Question_1 {
    public static void main(String[] args) {

      /*  public int searchAccount( int[25] number ){ // FIXME We should not give the length in the first square bracket
            number = new int[15];  // FIXME we have to declare the array like this => int[] number = new int[15];
            for (int i = 0; i < number.length; i++)
                number[i] = number[i - 1] + number[i + 1]; // fixme "number[i -1] this will throw error => 0-1 will be negative
            return number;*/
        }
}
